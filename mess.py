# import sqlite3
# # car = datetime.datetime.now()
# # far = f"{car.day}-{car.month}-{car.year}  {car.hour}:{car.minute}"
# conn = sqlite3.connect('comments.db')
# c = conn.cursor()
# c.execute('''create table post(
#             name message_text ,
#             comment message_text,
#             time message_text
#
# )''')
# conn.commit()
# conn.close()